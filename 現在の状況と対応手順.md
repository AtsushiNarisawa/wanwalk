# WanWalk v2 現在の状況と対応手順

**最終更新**: 2025-12-17

---

## 📊 現在の状況まとめ

### ✅ 修正完了した問題

1. **ピン投稿時のtype 'Null' is not a subtype of type 'String'エラー**
   - ✅ `RoutePin.fromJson`で`route_id`を`String?`に変更
   - ✅ commit: `34ab340`
   - ✅ GitHubにプッシュ済み

2. **Bottom Sheet最小化時のOverflowエラー**
   - ✅ `_minHeight`を80px→110pxに調整
   - ✅ `_buildEmptyState`を`SingleChildScrollView`でラップ
   - ✅ commit: `d8f64d4`, `581f058`
   - ✅ GitHubにプッシュ済み

3. **近くのおすすめルートが表示されない問題（一部）**
   - ✅ `skip(1)`を削除し、全ルートをBottom Sheetに表示
   - ✅ 検索範囲を20km→50kmに拡大
   - ✅ デバッグログを追加
   - ✅ commit: `d8f64d4`, `581f058`
   - ✅ GitHubにプッシュ済み

---

## 🚨 **未解決の問題（最優先）**

### 問題1: ピン投稿は成功するが、HOME画面に表示されない

**エラー内容:**
```
PostgrestException(message: column official_routes.title does not exist, code: 42703)
```

**原因:**
- Supabaseの`get_recent_pins`関数が古いまま
- データベース側の更新が必要

**✅ 解決方法（必須作業）:**

#### 手順1: Supabase SQL Editorにアクセス

ブラウザで以下を開く:
```
https://supabase.com/dashboard/project/jkpenklhrlbctebkpvax/editor
```

#### 手順2: 既存の関数を削除

以下のSQLを実行:

```sql
DROP FUNCTION IF EXISTS get_recent_pins(INT, INT);
```

#### 手順3: 新しい関数を作成

以下のSQLを実行:

```sql
CREATE OR REPLACE FUNCTION get_recent_pins(
  p_limit INT DEFAULT 10,
  p_offset INT DEFAULT 0
)
RETURNS TABLE (
  pin_id UUID,
  route_id UUID,
  route_name TEXT,
  area_id UUID,
  area_name TEXT,
  prefecture TEXT,
  pin_type TEXT,
  title TEXT,
  comment TEXT,
  likes_count INT,
  comments_count INT,
  photo_url TEXT,
  user_id UUID,
  user_name TEXT,
  user_avatar_url TEXT,
  created_at TIMESTAMPTZ,
  pin_lat FLOAT,
  pin_lon FLOAT
)
LANGUAGE plpgsql
AS $$
BEGIN
  RETURN QUERY
  SELECT 
    rp.id AS pin_id,
    rp.route_id,
    official_routes.title AS route_name,
    official_routes.area_id,
    areas.name AS area_name,
    areas.prefecture,
    rp.pin_type,
    rp.title,
    rp.comment,
    rp.likes_count,
    0 AS comments_count,
    (
      SELECT rpp.photo_url
      FROM route_pin_photos rpp
      WHERE rpp.pin_id = rp.id
      ORDER BY rpp.display_order ASC
      LIMIT 1
    ) AS photo_url,
    rp.user_id,
    COALESCE(auth.users.raw_user_meta_data->>'display_name', 'Unknown User') AS user_name,
    COALESCE(auth.users.raw_user_meta_data->>'avatar_url', '') AS user_avatar_url,
    rp.created_at,
    ST_Y(rp.location::geometry) AS pin_lat,
    ST_X(rp.location::geometry) AS pin_lon
  FROM route_pins rp
  LEFT JOIN official_routes ON official_routes.id = rp.route_id
  LEFT JOIN areas ON areas.id = official_routes.area_id
  LEFT JOIN auth.users ON auth.users.id = rp.user_id
  WHERE EXISTS (
    SELECT 1 FROM route_pin_photos rpp WHERE rpp.pin_id = rp.id
  )
  ORDER BY rp.created_at DESC
  LIMIT p_limit
  OFFSET p_offset;
END;
$$;
```

#### 手順4: 動作確認

以下のSQLで確認:

```sql
SELECT * FROM get_recent_pins(2, 0);
```

エラーが出なければOK（データが0件でも問題なし）

#### 手順5: Flutterアプリで確認

Mac側で:
```bash
cd ~/projects/webapp/wanwalk
git pull origin main
flutter run
```

---

### 問題2: 近くのおすすめルートが0件

**現在の状況:**
- 現在地: `35.25241577122233,139.13942730090932` (神奈川県小田原市付近)
- 全ルート数: 13件
- 50km以内のルート: **0件** ← 問題

**原因（推測）:**
1. ルートデータの`start_location`が遠すぎる
2. または座標データに問題がある

**✅ 調査方法:**

Supabase SQL Editorで以下を実行:

```sql
-- 現在地から各ルートまでの距離を計算
WITH current_location AS (
  SELECT ST_SetSRID(ST_MakePoint(139.13942730090932, 35.25241577122233), 4326)::geography as point
)
SELECT 
  r.id,
  r.title,
  ST_Y(r.start_location::geometry) as start_lat,
  ST_X(r.start_location::geometry) as start_lon,
  ROUND(
    ST_Distance(
      r.start_location,
      (SELECT point FROM current_location)
    )::numeric / 1000,
    2
  ) as distance_km
FROM official_routes r
WHERE r.is_active = TRUE
ORDER BY distance_km ASC
LIMIT 20;
```

**この結果をチャットに貼り付けてください！**

---

## 📋 次のステップ（優先順位順）

### ⚡ 最優先（今すぐ実施）

1. **Supabase SQL Editorで`get_recent_pins`関数を更新**
   - 上記の手順1〜4を実行
   - これを実行しないとHOME画面のエラーが解消されません

2. **ルートデータの距離調査SQLを実行**
   - 上記のSQLを実行して結果を確認
   - なぜ50km以内に0件なのかを特定

3. **Mac側でアプリを完全再起動**
   ```bash
   cd ~/projects/webapp/wanwalk
   git pull origin main
   flutter clean
   flutter pub get
   flutter run
   ```

### 🔍 確認項目

実行後、以下を確認してください:

#### ✅ HOME画面
- [ ] 「最新のピン投稿」セクションでエラーが出ない
- [ ] 投稿したピンが正しく表示される
- [ ] 新しいピンを投稿すると即座に反映される

#### ✅ MAP画面
- [ ] Bottom Sheetが正しく動作する（110px/300px/500px）
- [ ] Bottom Sheet最小化時にOverflowエラーが出ない
- [ ] 「近くのおすすめルート」に何件か表示される（理想）
- [ ] または「現在地から50km以内におすすめルートがありません」と表示される

#### ✅ ログ確認
Flutter実行時のログで以下を確認:
```
🔵 _getRecommendedRoutes: currentLocation=35.252,139.139
🔵 Total routes: 13
🔵 Route: ○○コース at XX.XXX,XXX.XXX - XX.Xkm  ← このログが出ているか
🔵 Total nearby routes (<=50km): X  ← 何件あるか
```

---

## 📂 関連ファイル

### コードの修正箇所
- `lib/models/route_pin.dart` - `route_id`の型をString?に変更
- `lib/screens/main/tabs/map_tab.dart` - Bottom Sheet調整、検索範囲50km
- `lib/screens/outing/pin_create_screen.dart` - ピン投稿後のリフレッシュ処理

### データベース
- `supabase_migrations/009_fix_get_recent_pins_null_route.sql` - 修正版SQL
- Supabase SQL Editorで手動実行が必要

### ドキュメント
- `SUPABASE_FIX_INSTRUCTIONS.md` - Supabase修正の詳細手順
- `DEBUG_ROUTES_DATA.md` - ルートデータのデバッグ手順
- `現在の状況と対応手順.md` - このファイル

---

## 💡 よくある質問

### Q1: SQLを実行したのにエラーが続く

**A:** 以下を確認してください:
1. 正しいSupabaseプロジェクト（jkpenklhrlbctebkpvax）で実行したか
2. SQL実行後に「Success」と表示されたか
3. Flutterアプリを完全再起動したか（Hot Restartではなく完全停止→再実行）

### Q2: 近くのルートが0件のまま

**A:** これは正常な可能性があります。以下を確認:
1. 上記の距離調査SQLを実行
2. 最も近いルートが何km離れているかを確認
3. もし100km以内にルートがあれば、検索範囲を拡大する修正を提案します

### Q3: ピン投稿は成功するが、ライブラリに保存されない

**A:** これは**仕様です**。
- WanWalkはSupabase Storageにアップロード
- iPhoneの写真ライブラリには自動保存されません
- SNSアプリの一般的な動作です

---

## 🎯 ゴール

### 最終的に確認したい状態

1. ✅ ピン投稿が成功する
2. ✅ 投稿したピンがHOME画面の「最新のピン投稿」に即座に表示される
3. ✅ MAP画面でOverflowエラーが出ない
4. ✅ MAP画面のBottom Sheetが正常に動作する
5. ⚠️ 近くのおすすめルートの表示（データ次第）

---

## 📞 次のアクション

**篤志さんにお願いしたいこと:**

1. **Supabase SQL Editorで上記のSQLを実行**（最優先）
2. **ルート距離調査のSQLを実行して結果を共有**
3. **Mac側でgit pull & flutter run**
4. **実行結果のログとスクリーンショットを共有**

その後、残っている問題があれば対応します！

---

**ファイルの場所**: `/home/user/webapp/wanwalk/現在の状況と対応手順.md`

**GitHub**: すべての修正はpush済み
**最新commit**: `451424c`
